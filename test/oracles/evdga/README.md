eVDGA oracle fixtures live here.

The first eVDGA oracle pass is intentionally lightweight: workflow tests
render the bundle, decode the oracle plan and report with Effect Schema, and
run the shared local FHIR checks through the integration API.
