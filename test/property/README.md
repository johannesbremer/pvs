# Property Tests

Planned property-test layers:

- Effect Schema encode/decode laws for shared primitives and canonical DTOs
- canonical domain invariants
- family-specific codec properties
- oracle-backed output properties

Shrunk failing seeds should be persisted as replayable fixtures in `test/oracles`.
